package com.example.booking_apartments;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class BookingApartmentsApplicationTests {



    @Test
    void contextLoads()  {
        /*Mockito.when(geoCoderManagerService.requestToGeo(URL_TEST)).thenReturn(GEO_RESPONSE_VALUE);
        mockMvc.perform(MockMvcRequestBuilders.get(GET_APARTMENTS_BY_LOCATION)
                        .param(LATITUDE, LONGITUDE))
                .andExpect(MockMvcResultMatchers.status().is(200));*/
    }

}
